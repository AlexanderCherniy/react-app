let initialState = {
    headerLinks: [
        {id:1,text:'OTHER'},
        {id:2,text:'HELP'},
        {id:3,text:'SETTINGS'},
    ],
}

let reducer = (state = initialState,action)=>{
    return state
}
export default reducer